id: io.github.raspbeguy.Terrain
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang
build-options:
  # ccache must precede the SDK's gcc on PATH or cgo bypasses the cache.
  prepend-path: /usr/lib/sdk/ccache/bin
  append-path: /usr/lib/sdk/golang/bin
  env:
    # Vendor mode keeps the build offline (Flathub requirement).
    GOFLAGS: -mod=vendor
    # Fail fast if go.mod outpaces the SDK's Go instead of network-fetching.
    GOTOOLCHAIN: local
command: terrain
cleanup:
  - /include
  - /lib/pkgconfig
  - /share/man
  - '*.la'
  - '*.a'
finish-args:
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --filesystem=home
  - --talk-name=org.freedesktop.secrets
  - --talk-name=org.freedesktop.Flatpak
modules:
  - name: terrain
    buildsystem: meson
    sources:
      - type: dir
        path: ../..
      # Regenerate via flatpak-go-mod after dependency bumps.
      - go.mod.yml
